@model OSBLE.Models.Assignment

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Assignment</legend>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).Description)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).Description)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).ReleaseDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).ReleaseDate)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).ReleaseDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).DueDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).DueDate)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).DueDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).isGradeable)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).isGradeable)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).isGradeable)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).isTeam)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).isTeam)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).isTeam)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).InstructorCanReview)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).InstructorCanReview)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).InstructorCanReview)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).WeightID, "Weight")
        </div>
        <div class="editor-field">
            @Html.DropDownList("WeightID", String.Empty)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).WeightID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => (model as OSBLE.Models.Assignment).PossiblePoints)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => (model as OSBLE.Models.Assignment).PossiblePoints)
            @Html.ValidationMessageFor(model => (model as OSBLE.Models.Assignment).PossiblePoints)
        </div>
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
