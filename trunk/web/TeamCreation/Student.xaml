<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d"
    x:Class="TeamCreation.Student"
    d:DesignWidth="640" d:DesignHeight="480" DataContext="{Binding}">

    <Grid x:Name="LayoutRoot">
        <Border Height="25" BorderBrush="Black" BorderThickness="1"  VerticalAlignment="Top" MinWidth="180">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="{Binding color1}" Offset="0"/>
                    <GradientStop Color="{Binding color2}" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <TextBlock TextWrapping="Wrap" Text="Joe Student" VerticalAlignment="Top" Foreground="{Binding textColor}" Margin="8,3,8,0" HorizontalAlignment="Left" Width="150" Name="StudentName" />
        </Border>

    </Grid>
</UserControl>