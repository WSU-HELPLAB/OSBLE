<UserControl x:Class="ReviewInterfaceBase.View.Document.XpsDocumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    >

    <!--We a need a grid to ensure a real height and width canvas aren't so good at that-->
    <Grid Grid.Row="1" x:Name="LayoutRoot" Background="LightGray">
        <Canvas x:Name="CommentReferenceCanvas" Canvas.ZIndex="255"/>
        <Canvas x:Name="HitCanvas" Canvas.ZIndex="255"/>
    </Grid>
</UserControl>