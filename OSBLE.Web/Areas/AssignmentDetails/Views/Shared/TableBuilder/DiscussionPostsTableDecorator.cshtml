@using OSBLE.Resources
@using OSBLE.Models.Assignments
@model DynamicDictionary
@{
    dynamic dataModel = Model;
    IAssignmentTeam currentTeam = dataModel.AssignmentTeam as IAssignmentTeam;      
    String postCount = dataModel.Posts.PostCount.ToString();
}
<td>  
     @(Html.ActionLink(postCount,
     "TeacherIndex", "DiscussionAssignment",
     new
     {
         assignmentId = dataModel.Posts.AssignmentID,
         courseUserId = dataModel.Posts.CourseUserID,
         discussionTeamId = dataModel.Posts.DiscussionTeamID,
         highlightValueString = "Posts",
         area = ""
     }, new { }))
</td>
