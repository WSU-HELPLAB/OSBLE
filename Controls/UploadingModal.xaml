<controls:ChildWindow x:Class="FileUploader.Controls.UploadingModal"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           Width="400" Height="300" 
           Title="Uploading">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" >
            <TextBlock Text="Uploading..." FontSize="28" HorizontalAlignment="Center" Margin="0,15,0,0"/>
            <ProgressBar  x:Name="UploadProgressBar" Width="300" Height="20" Margin="0,40"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Currently Uploading: "/>
                <TextBlock x:Name="UploadingFile" Text="MyDocuments" MaxWidth="200" Margin="15,0,0,0" />
            </StackPanel>
        </StackPanel>
        

        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
    </Grid>
</controls:ChildWindow>