@using OSBLE.Resources
@using OSBLE.Models.Assignments
@using OSBLE.Models.Courses
@model DynamicDictionary
@{
    dynamic dataModel = Model;
    //CourseUser cu = dataModel.CRdownload.student;
    int i = 0;
    //TODO: replace dummy link with working link
}

<tr>
    <th>Submission to Review</th>
    <td>        
        @(Html.ActionLink("Change Document to review", "GetReviewsOfAuthor", "FileHandler",
        new { area = "", assignmentId = 1, receiverId = 2 },
        new { onclick = "create_and_open_downloading_submission_dialog()" }))        
    </td>
</tr>
